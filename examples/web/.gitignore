# built by build.sh: the wasm-bindgen output and the scenes copied from examples/json
www/pkg/
www/scenes/
/target

# generated by `gen_bench_data`, tens of megabytes, see www/compare.html
www/*.arrow
