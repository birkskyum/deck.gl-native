// The path GitHub Pages serves the site from. Both the Astro config and the plugin that
// rewrites links in the prose read it from here, so there is one place to change it.
export const base = '/deck.gl-native'
